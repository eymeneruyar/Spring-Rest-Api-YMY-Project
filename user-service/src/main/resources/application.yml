server:
  port: 8081 #TODO: Servisler yapılandırıldıktan sonra port'lar değişecek.

spring:
  application:
    name: user-service
  datasource:
    username: postgres
    password: 198658
    url: jdbc:postgresql://localhost:5432/yarbac_new
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true
    show-sql: true

  mail:
    host: localhost
    port: 1025
    username: hello
    password: hello

    properties:
      mail:
        smtp:
          ssl:
            trust: "*"
          auth: true
          starttls:
            enable: true
          connectiontimeout: 5000
          timeout: 3000
          writetimeout: 5000
