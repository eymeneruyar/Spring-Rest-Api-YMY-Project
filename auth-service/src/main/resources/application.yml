server:
  port: 8080

spring:
  application:
    name: auth-service
  datasource:
    username: postgres
    password: 198658
    url: jdbc:postgresql://localhost:5432/yarbac_new
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true
    show-sql: true
